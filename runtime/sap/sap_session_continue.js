const {requireSafeSessionState}=require('./sap_dom_guard');const {preserveExistingSessions}=require('./sap_session_preserve');
async function findContinue(page){let rows;if(typeof page.evaluate==='function')rows=await page.evaluate(()=>Array.from(document.querySelectorAll('a,button,input[type=button],input[type=submit]')).map((e,index)=>({index,visible:!!(e.offsetWidth||e.offsetHeight||e.getClientRects().length),text:(e.innerText||e.value||'').replace(/\s+/g,' ').trim()})));else{const candidates=page.locator('a,button,input[type=button],input[type=submit]');rows=await candidates.evaluateAll(es=>es.map((e,index)=>({index,visible:!!(e.offsetWidth||e.offsetHeight||e.getClientRects().length),text:(e.innerText||e.value||'').replace(/\s+/g,' ').trim()})));}const hits=rows.filter(x=>x.visible&&x.text.toLocaleLowerCase('tr-TR')==='devam');if(hits.length!==1)throw new Error('SAFE_ABORT_EXISTING_SESSION:continue-control-not-unique');return page.locator('a,button,input[type=button],input[type=submit]').nth(hits[0].index);}
async function continueWithoutClosingOthers(page){const preserved=await preserveExistingSessions(page);await requireSafeSessionState(page);const control=await findContinue(page);await requireSafeSessionState(page);await control.click({timeout:3000});return {continued:true,preserved};}module.exports={continueWithoutClosingOthers,findContinue};

const {firefox}=require('/tmp/pw-firefox-test/node_modules/playwright');const {buildSearchPlan}=require('./sap_search_plan');
(async()=>{const b=await firefox.launch({headless:true});try{const p=await b.newPage();await p.setContent(`<style>label{display:inline-block;width:240px}input{width:180px}</style><div><label>Kayıt tarihi (zaman çerçevesi)</label><input id="date"></div><div><label>Ürün tanıtıcısı</label><input id="product"></div><div><label>Azami sonuç sayısı</label><input id="max"></div>`);const x=await buildSearchPlan(p);console.log(JSON.stringify({ok:true,ids:Object.fromEntries(Object.entries(x).map(([k,v])=>[k,v.id]))}));}finally{await b.close();}})().catch(e=>{console.error(e.message);process.exit(2)});

const {same,guard}=require('./sap_reconciliation');
async function applyRows(prisma,rows,{dryRun=false}={}){const ids=rows.map(r=>r.confirmationId),cutoff=new Date(Math.min(...rows.map(r=>new Date(r.recordDate).getTime())));const current=await prisma.sapConfirmation.findMany({where:{productId:'203',recordDate:{gte:cutoff}},select:{confirmationId:true,status:true,recordDate:true}}),existing=await prisma.sapConfirmation.findMany({where:{confirmationId:{in:ids}}}),by=new Map(existing.map(r=>[r.confirmationId,r])),g=guard(rows,current);let inserted=0,updated=0,unchanged=0,deleted=0;for(const r of rows){const e=by.get(r.confirmationId);if(!e)inserted++;else if(same(e,r))unchanged++;else updated++;}if(dryRun)return {dryRun:true,rows:rows.length,inserted,updated,unchanged,deleted:0,wouldDelete:g.allowDelete?g.missingCurrent:0,blockedDeletes:g.allowDelete?0:g.missingCurrent,deleteGuard:g,cutoff};await prisma.$transaction(async tx=>{for(const r of rows){const e=by.get(r.confirmationId);if(!e)await tx.sapConfirmation.create({data:r});else if(!same(e,r))await tx.sapConfirmation.update({where:{confirmationId:r.confirmationId},data:{...r,sourceSyncedAt:new Date()}});}if(g.allowDelete){const res=await tx.sapConfirmation.deleteMany({where:{productId:'203',recordDate:{gte:cutoff},confirmationId:{notIn:ids}}});deleted=res.count;}},{timeout:60000});return {dryRun:false,rows:rows.length,inserted,updated,unchanged,deleted,blockedDeletes:g.allowDelete?0:g.missingCurrent,deleteGuard:g,cutoff};}
module.exports={applyRows};

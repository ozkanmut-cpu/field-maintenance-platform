function tr(s){return (s||'').toLocaleLowerCase('tr-TR').replace(/\s+/g,' ').trim();}
async function visibleTextMatches(frame,text){const target=tr(text);return frame.evaluate(t=>Array.from(document.querySelectorAll('body *')).filter(e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();if(e.children.length)return false;const x=(e.innerText||e.value||'').toLocaleLowerCase('tr-TR').replace(/\s+/g,' ').trim();return s.visibility!=='hidden'&&s.display!=='none'&&r.width>0&&r.height>0&&x===t;}).length,target);}
async function findUniqueFrameForText(page,text){const hits=[];for(const [i,f] of page.frames().entries()){const n=await visibleTextMatches(f,text).catch(()=>0);if(n)hits.push({index:i,count:n,frame:f});}const total=hits.reduce((a,x)=>a+x.count,0);if(total!==1)throw new Error(`SAFE_ABORT_DOM:text-not-unique:${text}:${total}`);return hits[0].frame;}module.exports={tr,visibleTextMatches,findUniqueFrameForText};
